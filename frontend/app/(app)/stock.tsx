import { TabPlaceholder } from '@/components/TabPlaceholder';
export default function Stock() {
  return <TabPlaceholder title="Stock" description="Inventory list with par levels, par stock, and live counts. Port from app/MainApp.jsx StockTab." />;
}
