import { Stub } from '@/components/Stub';
export default function Biometric() {
  return <Stub step={12} title="Enable Face ID / fingerprint" nextHref="/onboarding/billing" note="expo-local-authentication. Use authenticateAsync()." />;
}
