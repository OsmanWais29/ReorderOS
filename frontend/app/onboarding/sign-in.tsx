import { Stub } from '@/components/Stub';
export default function SignIn() {
  return <Stub step={1} title="Sign in" nextHref="/(app)/home" note="Email + password, Sign in with Apple, magic link. Port from web." />;
}
